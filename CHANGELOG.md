# 업데이트 내역

날짜는 배포 기준(KST)입니다. 최신 항목이 위에 옵니다.

---

## 2026-07-26 — 검색 · 작품/캐릭터 분류 · 유리 UI

이번 업데이트의 큰 줄기는 **사진을 찾는 길을 늘린 것**입니다.
전에는 행사(컬렉션)와 모델로만 들어갈 수 있었는데, 이제 작품·캐릭터·통합 검색이 더해졌습니다.

### 통합 검색

- 내비게이션에 `⌕` 아이콘을 넣었습니다. 모바일에서도 햄버거 옆에 그대로 보입니다.
- 키보드 `/` 로 바로 열리고, `↑ ↓` 로 이동, `Enter` 로 이동, `ESC` 로 닫힙니다.
- 행사 · 작품 · 캐릭터 · 모델을 한 번에 찾습니다. 색인(`/api/search-index`)을 한 번 받아
  브라우저에서 걸러내기 때문에 글자를 칠 때마다 서버를 다시 부르지 않습니다.
- **한국어 통칭 검색어(별칭)**: 공식 행사명이 영어여도 사람들이 쓰는 말로 찾힙니다.
  - `Comic World` → 서코 · 부코 · 수코 · 코믹월드
  - `PlayX4` → 플엑 · 플레이엑스포
  - 별칭은 관리자 페이지에서 직접 추가/삭제할 수 있고, 행사·작품·모델 모두 지원합니다.
- 별칭은 행사 **이름** 기준입니다. 그래서 날짜가 다른 `Comic World` 3개가 `서코` 하나로 모두 찾힙니다.
  (전에는 회차별 id에 걸려 있어 한 회차만 걸렸습니다 — `migrations/0007`)

### 작품(장르) · 캐릭터

- 폴더에 **작품** 항목이 생겼습니다. 캐릭터 표기 `작품 - 캐릭터` 에서 자동으로 뽑아내고,
  필요하면 관리자가 직접 지정해 덮어쓸 수 있습니다.
- 원작이 없는 캐릭터는 **그 캐릭터 이름 자체가 작품**이 됩니다.
  (`오리지널` 같은 묶음은 두지 않았습니다. "코스플레이어의 창작 캐릭터"와
  "원작이 없는 캐릭터"를 구분해주지 못해 오히려 헷갈리기 때문입니다.)
- `Characters` 페이지를 추가했습니다. 내비 순서는 Collections → Photos → Models → Characters → About.
- 3단으로 파고들 수 있습니다: **작품 목록 → 작품 → 캐릭터**
  - 기본 정렬은 **가나다순**(칩이 왼쪽), 다른 하나는 사진 많은 순.
  - **그리드 / 목록** 보기를 오른쪽 작은 아이콘으로 고를 수 있고, 선택은 기억됩니다.
  - 그리드의 **작품 대표 사진을 관리자가 직접 지정**할 수 있습니다. 지정하지 않으면 첫 사진을 씁니다.
  - 캐릭터 상세는 **모델 단위**로 묶어 보여줍니다. (행사가 아니라 사람 기준)

### 모델 표기 규칙 통일

- **어느 화면에서든 닉네임이 주, 계정(`@핸들`)은 작게** 보조로 나옵니다.
- 여러 명이면 **닉네임은 `&`, 계정은 `,`** 로 구분합니다.
  - 예: `선혈 & 마요` / `@senketsu_l, @mignon_0807`
- 이름을 찾는 순서를 사이트 전체에서 하나로 맞췄습니다:
  **등록 이름 → 그 모델 단독 폴더 이름 → 합동 폴더 이름을 `&`로 쪼갠 조각 → `@핸들`**
  - 합동 폴더는 조각 수와 계정 수가 같을 때만 순서대로 짝지어 씁니다.
  - 덕분에 따로 이름을 등록하지 않은 모델도 `@핸들` 대신 닉네임으로 나옵니다.
- `Models` 목록은 **가나다순 고정**입니다. (사람을 찾는 화면이라 정렬을 고를 이유가 없었습니다)

### 유리(글래스) UI

- 블러를 쓰는 표면을 토큰(`--glass-bg` · `--glass-blur` · `--glass-line`)으로 묶어 **전부 같은 효과**로 통일했습니다.
- 모바일 메뉴가 그냥 투명하게 보이던 문제를 고쳤습니다.
  원인은 `.nav` 자체에 걸린 `backdrop-filter` 가 자식(드롭다운)의 블러를 무효화하는 것이었습니다.
  유리 표면을 `.nav::before` 로 분리해서, 드롭다운이 내비 바와 똑같은 블러를 갖게 했습니다.
- 검색 패널을 열면 글씨가 뭉개져 보이던 것도 함께 잡았습니다.
  (반투명 배경 위의 `backdrop-filter` 는 합성 레이어를 만들어 글자 안티에일리어싱을 망칩니다)

### 화면 다듬기

- 각 페이지 제목을 **대문자 + 넓은 자간**으로 맞췄습니다.
- 홈의 `Collections` 도 작은 라벨이 아니라 다른 페이지처럼 **가운데 큰 제목**으로 올렸습니다.
  `6 COLLECTIONS · ALL PHOTOS →` 처럼 개수도 함께 보여줍니다.
- 랜덤 슬라이드의 모델 계정이 전부 대문자로 나오던 것을 고쳤습니다.
- 로딩 표시를 `Loading…` 으로 통일했습니다.

### 조회수

- 관리자 로그인 상태와 `nostat` 쿠키가 있는 브라우저는 조회수에서 빠집니다.
  `/?nostat=1` 로 한 번 들어가면 그 브라우저는 이후 세지 않습니다. (`/?nostat=0` 으로 해제)
- 개발·확인 과정에서 부풀려진 값을 실제 방문 수준으로 정리했습니다.

---

## 2026-07-25 — 이동이 즉시 느껴지게

- 뒤로가기가 **1126ms → 26ms**. 전에는 이전 화면에 1초 가까이 머물다 넘어가서
  컬렉션마다 고유 주소가 없는 것처럼 보였습니다.
  화면을 비우기 전에 데이터를 먼저 받고, 느릴 때만 180ms 뒤에 로딩 표시를 띄웁니다.
- 관리자 페이지도 해시 라우팅으로 바꿔 **뒤로가기가 동작**합니다.
- 갤러리 사진을 **키보드로 열 수 있습니다**(`<button>` + 포커스 링). 라이트박스는
  `role="dialog"` · `aria-modal` 이고 포커스를 옮겼다가 닫을 때 되돌립니다.
- 모바일에서 라이트박스를 **뒤로가기로 닫습니다**. 전에는 사이트를 떠나버렸습니다.
- 미리보기(OG) 카드를 새 인트로 이미지로 다시 만들고, 제목을 프로젝트명으로 정리했습니다.
- 파비콘을 일식(림라이트) 모양으로 새로 만들었습니다.

## 2026-07-24 — 이미지와 관리자 작업 속도

- `/img/*` 가 사진 테이블을 전부 훑던 것을 **인덱스 조회**로 바꿨습니다.
  (`SCAN p` → `SEARCH p USING INDEX idx_photos_key_large`)
- 이미지 캐시를 **30일 + 진짜 304**로 바꿨습니다. 전에는 5분마다 다시 받아왔습니다.
- **1280px 중간 크기**를 추가해 홈 표지가 1.26MB → 약 60% 가벼워졌습니다.
- 홈 랜덤 슬라이드가 97장을 다 받던 것을 **40장 표본**으로 줄였습니다.
- 사진 목록의 전체 개수를 스크롤마다 다시 세지 않습니다(첫 페이지에서 한 번만).
- 관리자: **여러 장 한 번에 삭제/이동**, 스크롤·선택 유지, **동시 업로드 3개 + 진행률/취소**.
- 매일 새벽 3시(KST) 정리 작업(만료 세션·휴지통) 을 크론으로 돌립니다.

---

## 문서

- 처음부터 따라 할 수 있는 설치 안내는 [SETUP.md](SETUP.md) 에 있습니다.
- 프로젝트 소개와 구조는 [README.md](README.md) 를 봐주세요.
